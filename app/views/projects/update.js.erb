$("#title<%= @project.id %>").text("<%= @project.title %>");

// $(document).ready(function(){
// 	$('.projects').on('click', ".edit_project_button", function(){
// 		$(this).parents(".project").find("span.title").text("<%= @project.title %>")
// 	});
// })
